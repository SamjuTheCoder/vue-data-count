import DataCount from './components/DataCount.vue'

export default {
    // eslint-disable-next-line no-unused-vars
    install(Vue, options) {
     Vue.component("vue-data-count", DataCount);
    }
   };